= render 'facebook_login'
- if current_user
  = render 'nickname_form'
  .place-in-ranking
    Twoje miejsce w rankingu:
    = current_user.place_in_ranking
  .best-score
    Twój rekord:
    = current_user.best_score
