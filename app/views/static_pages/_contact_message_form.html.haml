#contact-message
  .span.contact-form-btn Uważam, że
  = form_for ContactMessage.new, remote: true do |f|
    = f.text_area :content, placeholder: "Twoje zdanie jest dla nas ważne", class: 'text', rows: '6'
    = f.submit "Wyślij", class: 'btn btn-default submit'



