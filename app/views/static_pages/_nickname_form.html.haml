= form_for [:api, current_user], format: :html, html: { id: "nickname-form"} do |f|
  Nickname:
  = f.text_field :nickname, maxlength: User::MAX_NICKNAME_LENGTH, value: '', placeholder: current_user.nickname
  = f.submit "Zmień", class: 'btn btn-default btn-xs'
