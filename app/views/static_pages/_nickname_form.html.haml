= form_for current_user, html: { id: "nickname-form"} do |f|
  Nickname:
  = f.text_field :nickname, maxlength: User::MAX_NICKNAME_LENGTH
  = f.submit "Zmień nickname", class: 'btn btn-default btn-xs'
